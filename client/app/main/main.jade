//navbar
div(layout="column" , layout-align="center center")
  md-content
    iframe(src='https://dl.dropboxusercontent.com/u/51841719/Games/BulletHell/index.html', style='border:0px #000000 none;', name='BulletHell', scrolling='no', frameborder='1', marginheight='px', marginwidth="320px", height="656px", width="960px")

    #disqus_thread
      script.
        /**
         * RECOMMENDED CONFIGURATION VARIABLES: EDIT AND UNCOMMENT THE SECTION BELOW TO INSERT DYNAMIC VALUES FROM YOUR PLATFORM OR CMS.
         * LEARN WHY DEFINING THESE VARIABLES IS IMPORTANT: https://disqus.com/admin/universalcode/#configuration-variables
         */
        /*
         var disqus_config = function () {
         this.page.url = PAGE_URL; // Replace PAGE_URL with your page's canonical URL variable
         this.page.identifier = PAGE_IDENTIFIER; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
         };
         */
        (function () { // DON'T EDIT BELOW THIS LINE
          var d = document, s = d.createElement('script');
          s.src = '//sinsgames.disqus.com/embed.js';
          s.setAttribute('data-timestamp', +new Date());
          (d.head || d.body).appendChild(s);
        })();
      noscript
        | Please enable JavaScript to view the
        a(href='https://disqus.com/?ref_noscript', rel='nofollow') comments powered by Disqus.

  //md-toolbar(layout-align="center center")
    md-toolbar-tools
      h1 Welcome to SINS Sins Is Not Steam

  //md-content
    section
      form
        label Test for socket.io
        section(layout='row')
          md-input-container.md-block
            label Thing
            input(type='text', placeholder='Add a new thing here.', ng-model='main.newThing')
          md-button(type='submit', ng-click='main.addThing()') Add New
    section
      h1 Test:
      md-card(ng-repeat='thing in main.awesomeThings', layout='row')
          a(href='#', tooltip='{{thing.info}}') {{thing.name}}
          md-button(type='button', ng-click='main.deleteThing(thing)') &times;

//footer
